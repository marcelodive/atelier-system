// Useless file, but I will stay with this for reference

// const fs = require('fs');
// const configPath = './server/config.json';
// const configFile = require(configPath);

// configFile.host = process.env.app_host || 3000;
// configFile.port = process.env.app_port || '0.0.0.0';

// fs.writeFile(configPath, JSON.stringify(configFile), function (err) {
//   if (err) return console.log(err);
//   console.log(JSON.stringify(configFile));
//   console.log('writing to ' + configPath);
//   require('./server/server.js').start();
// });

